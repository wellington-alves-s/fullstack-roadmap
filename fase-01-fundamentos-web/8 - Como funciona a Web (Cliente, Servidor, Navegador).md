🌐 Como funciona a Web: Cliente, Servidor e Navegador

A Web funciona com base na arquitetura cliente-servidor, que é o modelo fundamental da própria internet.

📌 1. O que é arquitetura Cliente-Servidor?

É um modelo de comunicação baseado em requisições (pedidos) e respostas:

Cliente → faz a requisição de um serviço.

Servidor → recebe a requisição, processa e envia uma resposta.

Rede → permite que essa comunicação aconteça.

Esse modelo está presente sempre que você acessa um site, envia uma mensagem ou utiliza um aplicativo online.

🖥️ 2. Cliente

O cliente é o dispositivo ou programa que solicita um serviço.

Na Web, normalmente o cliente é o navegador, como:

Google Chrome

Mozilla Firefox

Microsoft Edge

Quando você digita um endereço (URL) e pressiona Enter, o navegador envia uma requisição HTTP ao servidor pedindo a página desejada.

Exemplo:

Você acessa www.exemplo.com → o navegador envia um pedido ao servidor.

🗄️ 3. Servidor

O servidor é um computador (ou conjunto de computadores) que fornece serviços ou recursos na rede.

Ele pode:

Armazenar sites

Processar dados

Gerenciar bancos de dados

Enviar arquivos (HTML, CSS, imagens, vídeos etc.)

Quando recebe uma requisição:

Analisa o pedido.

Processa as informações.

Envia uma resposta ao cliente.

O servidor pode:

Atender a requisição (enviando os dados).

Negar a requisição (por falta de permissão, erro etc.).

🌍 4. Rede

A rede é o meio que permite a comunicação entre cliente e servidor.

Ela pode envolver:

Internet

Cabos de fibra óptica

Wi-Fi

Roteadores

Provedores de internet

É pela rede que os dados trafegam de um ponto ao outro.

🔄 5. Como acontece na prática (Passo a passo)

Quando você acessa um site:

O navegador (cliente) envia uma requisição HTTP.

A requisição viaja pela rede.

O servidor recebe e processa o pedido.

O servidor envia uma resposta HTTP.

O navegador interpreta os dados e exibe a página para você.

🧠 Complementando a explicação

Além do que foi explicado no vídeo, é importante entender alguns pontos:

🔹 Protocolo HTTP/HTTPS

A comunicação entre cliente e servidor na Web ocorre principalmente via:

HTTP (HyperText Transfer Protocol)

HTTPS (versão segura com criptografia)

🔹 DNS

Antes de acessar um site, o nome digitado (ex: google.com) precisa ser traduzido para um endereço IP por meio do sistema DNS.

🔹 Front-end e Back-end

Front-end → Parte visual exibida no navegador.

Back-end → Parte que roda no servidor e processa dados.

📌 Resumindo

A arquitetura cliente-servidor funciona assim:

O cliente pede.

O servidor responde.

A rede conecta os dois.

Esse modelo é a base do funcionamento da internet e da Web moderna.
